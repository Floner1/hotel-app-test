[build]
buildCommand = "pip install -r requirements.txt && cd site1 && python manage.py collectstatic --noinput && python manage.py migrate"

[deploy]
startCommand = "cd site1 && gunicorn site1.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/"
